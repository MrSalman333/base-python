name: main
on:
  pull_request:
    branches: ["master"]
jobs:
  run-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Test with pytest
        run: docker compose run --rm pytest
